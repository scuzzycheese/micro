#include "404.h"

int error404(struct argData *args, coStData *regs)
{
	/*
	PSOCK_BEGIN(&ws->po);

	PSOCK_SEND_STR(&ws->po, "<h1>Error: 404</h1>");
	PSOCK_SEND_STR(&ws->po, "<b>Page not found!<br/>");

	PSOCK_CLOSE(&ws->po);
	PSOCK_END(&ws->po);
	 */
}
